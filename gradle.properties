org.gradle.daemon=false
org.gradle.jvmargs=-Xmx3G

# Minecraft
mc_version=1.21.1

# Architectury
architectury_plugin_version=3.5.163
architectury_loom_version=1.14-SNAPSHOT
architectury_version=13.0.8

# NeoForge
neoforge_version=21.1.222

# Fabric
fabric_loader_version=0.18.5
fabric_api_version=0.116.9+1.21.1

# Mod
mod_name=HatsRenewed
# mod_version is the minor.patch portion. Major component is derived from mc_version (1.21.1 -> 21).
# Final version: <mcMinor>.<mod_version>-mc<mc_version>
mod_version=1.1
mod_id=hats
mod_group=me.guivnf.mods

enabled_platforms=neoforge,fabric

# Publishing (mod-publish-plugin)
# Tokens are NOT stored here. They are read from environment variables at publish
# time (see gradle/publishing.gradle and publish-secrets.sh). Fill in the project
# ids/slug below with the real CurseForge and Modrinth project identifiers.
mod_publish_version=1.1.0
curseforge_id=1526345
curseforge_slug=hats-renewed
modrinth_id=G7zR9HGM
